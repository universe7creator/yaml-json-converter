{
  "name": "yaml-json-converter",
  "version": "1.0.0",
  "description": "Convert YAML files to JSON and vice versa instantly. Clean, fast, no data loss.",
  "main": "api/process.js",
  "scripts": {
    "dev": "node dev-server.js",
    "vercel-build": "echo 'Build complete'"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
